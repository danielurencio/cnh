CREATE TABLE DATOS_LICITACIONES_LIC_MAT(
  ID_LICITANTE NUMBER(8,0),
  ID_MATRIZ NUMBER(8,0),
  Foreign key (ID_LICITANTE) references DATOS_LICITACIONES_LICITANTES(ID_LICITANTE),
  Foreign key (ID_MATRIZ) references DATOS_LICITACIONES_MATRICES(ID_MATRIZ),
  UNIQUE(ID_LICITANTE,ID_MATRIZ)
);
