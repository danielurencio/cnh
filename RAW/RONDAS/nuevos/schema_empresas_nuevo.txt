CREATE TABLE DATOS_LICITACIONES_EMPRESAS(
  ID_EMPRESA NUMBER(8,0),
  EMPRESA VARCHAR2(200),
  PAIS VARCHAR2(100),
  ID_MATRIZ NUMBER(8,0),
  CONSTRAINT ID_EMPRESA PRIMARY KEY (ID_EMPRESA),
  Foreign key (ID_MATRIZ) references DATOS_LICITACIONES_MATRICES(ID_MATRIZ)
);
