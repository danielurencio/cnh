CREATE TABLE RONDAS_INTERMEDIA(
  ID_LICITANTE NUMBER(8,0),
  ID_EMPRESA NUMBER(8,0),
  Foreign key (ID_LICITANTE) references RONDAS_LICITANTES(ID_LICITANTE),
  Foreign key (ID_EMPRESA) references RONDAS_EMPRESAS(ID_EMPRESA),
  UNIQUE(ID_LICITANTE,ID_EMPRESA)
);
