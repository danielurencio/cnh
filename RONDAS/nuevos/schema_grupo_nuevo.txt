CREATE TABLE DATOS_LICITACIONES_LIC_GPO(
  ID_LICITANTE NUMBER(8,0),
  ID_GRUPO NUMBER(8,0),
  Foreign key (ID_LICITANTE) references DATOS_LICITACIONES_LICITANTES(ID_LICITANTE),
  Foreign key (ID_GRUPO) references DATOS_LICITACIONES_GRUPOS(ID_GRUPO),
  UNIQUE(ID_LICITANTE,ID_GRUPO)
);
