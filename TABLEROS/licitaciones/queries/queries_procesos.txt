SELECT A.*, B.ID_EMPRESA, C.EMPRESA
FROM DATOS_LICITACIONES_PROCESOS A
LEFT JOIN DATOS_LICITACIONES_LIC_EMP B
ON A.ID_LICITANTE = B.ID_LICITANTE
LEFT JOIN DATOS_LICITACIONES_EMPRESAS C
ON B.ID_EMPRESA = C.ID_EMPRESA;
